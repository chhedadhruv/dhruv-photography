## What

<!-- What changed, in one or two sentences. -->

## Why

<!-- The reason it needed to change. -->

## Checks

- [ ] `yarn verify` passes locally (lint with zero warnings, typecheck, tests)
- [ ] `yarn build` succeeds
- [ ] No `eslint-disable`, `@ts-ignore`, or `any` added
- [ ] Content changes (if any) still parse against the Zod schemas
